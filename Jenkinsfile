pipeline {
    agent any
    stages { 
        stage('Example') {
            steps {
                echo 'Hello World'
            }
        }
				stage('Goodbye') {
					steps {
							echo "Goodbye"
					}
				}
    }
}
